# Erasmus Kernel Snapshot
Generated: Thu, Mar 12, 2026  1:51:47 PM
Project root: /c/Users/jerem/jchackett,me

Max size: 50000 bytes

## DIRECTORY STRUCTURE\n\n```\n.
.eleventy.js
.eleventyignore
.gitignore
.vscode
.vscode/settings.json
.vscode/tasks.json
ERASMUS
ERASMUS/ERASMUS
ERASMUS/ERASMUS/_kernels
ERASMUS/ERASMUS/_kernels/kernel-20260310-130828.md
ERASMUS/Erasmus-CRITICAL.md
ERASMUS/Erasmus-FEEDBACK.md
ERASMUS/Erasmus_README.md
ERASMUS/Erasmus_REBUILD.sh
keybindings.json
netlify.toml
package-lock.json
package.json
README.md
scratch
scripts
scripts/rebuild-kernel.cmd
scripts/run-eleventy.cmd
src
src/css
src/css/components
src/css/components/disclosure.css
src/css/styles.css
src/images
src/images/jchackett.jpg
src/index.md
src/js
src/js/base.js
src/js/disclosures.js
src/js/forms.js
src/_includes
src/_includes/components
src/_includes/components/about-me.njk
src/_includes/components/disclosure-group.njk
src/_includes/components/get-started.njk
src/_includes/components/services.njk
src/_layouts
src/_layouts/base.njk
```\n\n## FILE: src/_includes/components/about-me.njk

```
{# ===== Component: About-Me ===== #}

{% set items = [
  {
    title: "Get in Touch",
    content: "<p>Contact details, email, phone, or inquiry instructions can go here.</p>"
  },
  {
    title: "Availability",
    content: "<p>Scheduling, office format, or response-time notes can go here.</p>"
  }
] %}

{% set title = "ABOUT ME" %}
{% set ariaLabel = "About-me" %}

{% include "components/disclosure-group.njk" %}

{# ===== /Component: About-me ===== #}
```

## FILE: src/_includes/components/disclosure-group.njk

```
{# ===== Component: Disclosure Group ===== #}

<section class="disclosure-group" aria-label="{{ ariaLabel or title }}">
  <details>
    <summary class="disclosure-header">{{ title }}</summary>

    {% for item in items %}
      <details class="disclosure-item">
        <summary>{{ item.title }}</summary>

        <div class="content-block">
          {{ item.content | safe }}
        </div>
      </details>
    {% endfor %}
  </details>
</section>

{# ===== /Component: Disclosure Group ===== #}
```

## FILE: src/_includes/components/get-started.njk

```
<section class="disclosure-group" aria-label="Get Started">
  <details id="contact-me">
    <summary class="disclosure-header">
      CONTACT ME
    </summary>

    <div class="content-block">
      
      <p class="consult-intro">
Drop me an email using the form below. I'll respond to you in 24 hours.
</p>

<div class="consult-capture">
  <form name="consultation" method="POST" data-netlify="true" class="consult-form">
    <input type="hidden" name="form-name" value="consultation">

    <p>
      <input type="email" name="email" placeholder="Email address" required>
    </p>

    <p>
      <textarea name="message" rows="4" placeholder="Optional. Please provide any relevent information."></textarea>
    </p>

    <p>
      <label>
        <input type="checkbox" name="newsletter" checked>
        I also want to receive your newsletter.
      </label>
    </p>

    <p>
      <button type="submit" class="button">SEND</button>
    </p>
  </form>
</div>

<div class="consult-success" hidden>
  <p>
    Message received! Thank you.<br>
  </p>

  <p>
    Want to skip a step? Schedule your consultation right now.
  </p>

  <div id="my-cal-inline-consultation"></div>
</div>
```

## FILE: src/_includes/components/services.njk

```
<section class="disclosure-group" aria-label="Services">

<details>

<summary class="disclosure-header">
MY SERVICES
</summary>

<div class="content-block">

<p>
I offer psychotherapy services in Maine & New York, and I offer social-work infused consulting services worldwide. 
</p>

<details class="disclosure-item">
<summary>Individual Psychotherapy</summary>
<div class="content-block">
<hr class="rule dotted-blue-rule">

<p>
Talk therapy is for individuals who have been given a psychiatric diagnosis by a medical professional. My psychotherapy clients are often facing legal issues or a critical situation. 
In process of making our thoughts, behaviors, and moods the subject of close examination. At the heart of this process is the <em>compassionate interview</em> — a unique dialogue in which individuals are free to safely examine (and dismantle) their resistance to change.

I contract with Headway to accept the following insurances:
        <ul class="insurance-list">
          <li>Aetna</li>
          <li>Anthem BC/BS</li>
          <li>Cigna</li>
          <li>Oscar</li>
          <li>Oxford</li>
          <li>Point32 Healthcare</li>
          <li>United Healthcare</li>
        </ul>
<hr class="rule dotted-blue-rule">
Want to see if I take your insurance? Begin that process over on Headway.
<a class="button"
   href="https://care.headway.co/providers/jeremiah-hackett"
   target="_blank"
   rel="noopener noreferrer">
  Check Your Insurance
</a>
<hr class="rule dotted-blue-rule">
Want to start the conversation?
<a href="#contact-me" class="button" id="schedule-consultation">
  Message Me
</a>
</div>
</details>



<details class="disclosure-item">
<summary>Social Work-Informed Consulting</summary>
<hr class="rule dotted-blue-rule">
<div class="content-block">
<p>
Lorem Ipsum
</p>
</div>



</details>
<hr class="blue-rule">

</div>

</details>

</section>
```

## FILE: src/_layouts/base.njk

```
<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>{{ title or "Page Title" }}</title>

<link rel="stylesheet" href="/css/styles.css">
<link rel="stylesheet" href="/css/components/disclosure.css">
<link href="https://rsms.me/inter/inter.css" rel="stylesheet">
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Source+Serif+4:wght@400;500;600&display=swap">
<link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:wght@400;500;600&display=swap" rel="stylesheet">
 <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=EB+Garamond:wght@400;500;600&display=swap" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/linux-libertine-font@5.3.0/fonts/linux-libertine.css" />
</head>
<body>
  <div class="wrapper">
    <main class="card">
      <h1 class="site-title">{{ title }}</h1>
      {{ content | safe }}
    </main>




    <footer class="site-footer">
  <div class="footer-card">
    <p>© 2026 Jeremiah Hackett. All rights reserved.</p>
      This site was vibecrafted by hand with ChatGPT 5.2.<br>
      It uses <a href="https://www.11ty.dev/" target="_blank" rel="noopener noreferrer">Eleventy</a>
      and a quiet stack of open-source tools.<br>
      Source available on <a href="https://github.com/jc-hackett" target="_blank" rel="noopener noreferrer">GitHub</a>.
    </p>
  </div>
</footer>
  </div>

<script defer src="/js/base.js"></script>
<script defer src="/js/disclosures.js"></script>
<script defer src="/js/forms.js"></script></body>
</html>
```

## FILE: src/css/components/disclosure.css

```
/* ===== Disclosure Component ===== */

/* top-level disclosure trigger */
.disclosure-header {
  display: block;
  width: 100%;

  padding: 1rem;
  margin-bottom: 1rem;

  text-align: center;
  cursor: pointer;

  background: #ffffff;
  border: 1px solid #e5e5e5;
  border-radius: 3px;
  box-shadow: 5px 10px 14px rgba(0,0,0,0.30);
}

.disclosure-group > details[open] .content-block {
  padding: 0
}

.disclosure-group > details[open] > summary {
  background: var(--ink);
  color: #ffffff;
  border-color: var(--ink);

  box-shadow:
    0 0 0 5px rgba(31,78,216,0.05),
    0 12px 34px rgba(0,0,0,0.06);
}

/* hide default disclosure arrows */
summary {
  list-style: none;
  cursor: pointer;
}

summary,
.button,
.menu-option,
nav,
label {
  font-family: var(--ui-font);
}

summary::-webkit-details-marker {
  display: none;
}

/* spacing between disclosure sections */
.disclosure-group + .disclosure-group {
  margin-top: 2rem;
}

.disclosure-item {
  background: #ffffff;
  border: 1px solid rgba(31,78,216,0.12);
  border-radius: 6px;

  padding: 1.25rem;
  margin-top: 1.2rem;

  box-shadow:
    0 0 0 3px rgba(31,78,216,0.03),
    0 8px 18px rgba(31,78,216,0.12);
}

/* top-level disclosure trigger */
.disclosure-header {
  display: block;
  
  padding: 1rem;
  text-align: center;
  cursor: pointer;
  margin-bottom: 1rem;
}

/* ===== Open Disclosure Header ===== */

.disclosure-group > details[open] > summary {
  background: var(--blue);
  color: #ffffff;
  border-color: var(--blue);
}

/* nested disclosure trigger */
.disclosure-item > summary {
  cursor: pointer;
}

/* divider under title when open */
.disclosure-item[open] > summary {
  display: block;
  /* border-bottom: 2px dotted blue;
  padding-bottom: 0.5rem; */
  /* margin-bottom: 0.75rem; */
  transition: margin-bottom 140ms ease;
}

/* disclosure motion */
.disclosure-item .content-block {
  opacity: 0;
  transform: translateY(-4px);
  transition:
    opacity 160ms ease,
    transform 160ms ease;
  visibility: hidden;
  pointer-events: none;
}

.disclosure-item[open] .content-block {
  opacity: 1;
  transform: translateY(0);
  visibility: visible;
  pointer-events: auto;
}

/* ===== Get Started Form Fields ===== */

.consult-form input[type="email"],
.consult-form textarea {
  width: 100%;
  padding: 0.7rem 0.8rem;

  font-family: inherit;
  font-size: inherit;
  line-height: inherit;

  border: 2px dotted var(--blue);
  border-radius: 0;
  background: #fff;

  appearance: none;
  -webkit-appearance: none;
  box-shadow: none;
  outline: none;
}

.consult-form textarea {
  min-height: 130px;
  resize: vertical;
}

.consult-form input[type="email"]:focus,
.consult-form textarea:focus {
  border-color: var(--blue);
  box-shadow: none;
}

/* ===== Custom Checkbox ===== */

.consult-form input[type="checkbox"] {
  width: 1.2rem;
  height: 1.2rem;
  margin-right: 0.6rem;

  border: 2px dotted var(--blue);
  border-radius: 0;
  background: #fff;

  appearance: none;
  -webkit-appearance: none;
  box-shadow: none;
  outline: none;

  position: relative;
  vertical-align: -5px;
  cursor: pointer;
}

.consult-form input[type="checkbox"]:checked::after {
  content: "✓";
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);

  font-size: 0.95rem;
  color: var(--blue);
  font-weight: bold;
}


#my-cal-inline-consultation {
  width: 100%;
  min-height: 700px;
}
```

## FILE: src/css/styles.css

```
/* ===== Variables / Base ===== */
:root {
  --paper: #f4f1ea;
  --ink: #1f1e1c;
  --rule: #d8d2c7;
  --blue: #1b8bf6;
  --muted: rgba(31, 30, 28, 0.65);
  --btn: #5f7fd9;
  --btn-hover: #4f71d3;
  --card-border: rgba(31, 78, 216, 0.16);
  --card-shadow:
    0 0 0 5px rgba(31, 78, 216, 0.05),
    0 12px 34px rgba(0, 0, 0, 0.06);
  --focus-ring: 0 0 0 4px rgba(31, 78, 216, 0.25);
  --radius: 10px;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  -webkit-text-size-adjust: 100%;
}

body {
  margin: 0;
  background: #f3f3f3;
  color: var(--ink);
  font-family: "Source Serif 4", Georgia, serif;
  font-size: 19px;
  line-height: 1.75;
}

.wrapper {
  max-width: 560px;
  margin: clamp(2rem, 6vh, 4rem) auto;
  padding: 0 1.25rem;
}

/* ===== Cards ===== */
.card,
.footer-card {
  background: #ffffff;
  border: 1px solid #e5e5e5;
  border-radius: 3px;
  box-shadow: 5px 10px 14px rgba(0,0,0,0.30);
}

/* main card spacing */
.card {
  padding: 3rem 2.5rem;
}

/* footer card spacing */
.footer-card {
  padding: 1.5rem 2rem;
  text-align: center;
  max-width: 520px;
}

.footer-card p {
  margin: 0.5rem 0;
}

/* ===== Typography ===== */
h1,
.sub, {
  font-family: "Linux Libertine", "EB Garamond", serif;
  font-variant-caps: small-caps;
  text-align: center;
  font-weight: 600;
      letter-spacing: 0.2em;
}

h1 {
  margin: 0 0 0.75rem;
  font-size: 2.5rem;
  letter-spacing: 0.085em;
}

.sub {
  margin: 0 0 2rem;
  letter-spacing: 0.4em;
}

p {
  margin: 0 0 1.25rem;
  text-align: justify;
  max-width: 65ch;
  hyphens: auto;
}

p:last-child {
  margin-bottom: 0;
}

/* ===== Rules ===== */
.rule {
  display: block;
  width: 100%;
  border: 0;
  margin: 2rem 0;
}

.blue-rule {
  border-top: 3px solid var(--blue);
}

.dotted-blue-rule {
  border-top: 2px dotted var(--blue);
  margin: 0.4rem 0 1rem 0;
}

hr,
.blue-rule {
  display: block;
  width: 100%;
  height: 0;
  margin: 2rem 0;
  border: 0;
  border-top: 3px solid var(--blue);
}

.section-break {
  margin: 4rem 0;
  border: 0;
  border-top: 4px solid var(--blue);
}

/* ===== Buttons ===== */

.button,
.btn,
.coverage-button,
a.button,
button.button {

  
    display: block;
  width: fit-content;
  margin: 1.5rem auto;
  display: block;
  align-items: center;
  justify-content: center;

  padding: 0.7rem 1.25rem;

  background: var(--blue);
  color: #ffffff;

  border: 1px solid var(--blue);
  border-radius: 6px;
  font-size: small;

  text-decoration: none;
  cursor: pointer;

  box-shadow: none;
  transition: background 0.15s ease;
}

.button:hover,
.btn:hover,
.coverage-button:hover,
a.button:hover,
button.button:hover {

  background: #183fc2;   /* darker version of your blue */
  border-color: #183fc2;
}

.button:focus-visible,
.btn:focus-visible,
.coverage-button:focus-visible,
a.button:focus-visible,
button.button:focus-visible {

  outline: 2px solid var(--blue);
  outline-offset: 2px;
}

.button:active,
.btn:active,
.coverage-button:active,
a.button:active,
button.button:active {

  transform: none;
}
/* ===== Profile Image ===== */
.profile-photo {
  display: block;
  width: 300px;
  margin: 1.5rem auto;
  object-fit: cover;
}

/* ===== Footer ===== */
.site-footer {
  display: flex;
  justify-content: center;
  margin-top: 3rem;
  padding-top: 1.5rem;
  padding-bottom: 2rem;
  text-align: center;
  color: #666;
  font-size: 0.95rem;
}
```

## FILE: src/index.md

```
---
title: Jeremiah Hackett
layout: base.njk
---

<div class="sub">CLINICAL SOCIAL WORKER</div>

<hr class="blue-rule">
<div id="services-intro">

<p>
I work with college-aged men and young professionals. My clients are typically concerned about their relationships, behaviors or moods. Most are being encouraged by loved ones to talk to someone.
</p>
<p>
And talking really can help.
</p>
<p>
Speaking without the fear of recrimination to a compassionate, interested listener is a timeless process. It helps us see and think differently. It disrupts rigid and uncharitable ways of thinking. 

It can help bring us relief.</p></div>
<hr class="blue-rule" id="services-reset">
{% include "components/services.njk" %}

{% include "components/about-me.njk" %}

{% include "components/get-started.njk" %}

```

## FILE: src/js/base.js

```
/* ===== Boot / Diagnostics ===== */
console.log("JS loaded");

/* ===== Schedule Consultation Button ===== */
document.addEventListener("DOMContentLoaded", () => {
  const scheduleLink = document.querySelector("#schedule-consultation");
  const contactDetails = document.querySelector("#contact-me");

  if (!scheduleLink || !contactDetails) return;

  scheduleLink.addEventListener("click", function () {
    contactDetails.open = true;
  });
});

/* ===== Menu Controller ===== */
(function menuController() {
  const options = Array.from(document.querySelectorAll(".menu-option"));
  const panels = Array.from(document.querySelectorAll(".menu-panel"));

  if (!options.length || !panels.length) return;

  function setActivePanel(index) {
    options.forEach((option, i) => {
      const isActive = i === index;
      option.classList.toggle("active", isActive);
      option.setAttribute("aria-selected", String(isActive));
    });

    panels.forEach((panel, i) => {
      const isActive = i === index;
      panel.classList.toggle("active", isActive);
      panel.hidden = !isActive;
    });
  }

  options.forEach((option, index) => {
    option.addEventListener("click", function () {
      const isAlreadyOpen = option.classList.contains("active");

      if (isAlreadyOpen) {
        options.forEach((btn) => {
          btn.classList.remove("active");
          btn.setAttribute("aria-selected", "false");
        });

        panels.forEach((panel) => {
          panel.classList.remove("active");
          panel.hidden = true;
        });

        return;
      }

      setActivePanel(index);
    });
  });

  panels.forEach((panel, index) => {
    panel.hidden = !panel.classList.contains("active");

    if (panel.classList.contains("active")) {
      options[index]?.classList.add("active");
      options[index]?.setAttribute("aria-selected", "true");
    }
  });
})();
```

## FILE: src/js/disclosures.js

```
/* ===== Top-Level Disclosure: One Open At A Time ===== */
(function disclosureController() {
  const groups = Array.from(document.querySelectorAll(".disclosure-group > details"));

  if (!groups.length) return;

  groups.forEach((group) => {
    group.addEventListener("toggle", function () {
      if (!group.open) return;

      groups.forEach((otherGroup) => {
        if (otherGroup !== group) {
          otherGroup.open = false;
        }
      });
    });
  });
})();

/* ===== Top-Level Header Focus ===== */
document.addEventListener("DOMContentLoaded", () => {
  const topDetails = document.querySelectorAll(".disclosure-group > details");
  const firstRule = document.querySelector("#services-reset");

  function scrollToTarget(el) {
    if (!el) return;

    const top = el.getBoundingClientRect().top + window.scrollY - 18;

    window.scrollTo({
      top,
      behavior: "smooth"
    });
  }

  topDetails.forEach((details) => {
    const summary = details.querySelector(":scope > summary");
    if (!summary) return;

    summary.addEventListener("click", () => {
      const willOpen = !details.open;

      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          if (willOpen) {
            scrollToTarget(summary);
          } else if (firstRule) {
            scrollToTarget(firstRule);
          }
        });
      });
    });
  });
});

/* ===== Services Intro / Hidden Reset ===== */
document.addEventListener("DOMContentLoaded", () => {
  const intro = document.querySelector("#services-intro");
  const servicesTop = document.querySelector('section[aria-label="Services"] > details');
  const subItems = document.querySelectorAll(
    'section[aria-label="Services"] details.disclosure-item'
  );
  const reset = document.querySelector("#services-reset");

  if (!intro || !servicesTop || !reset) return;

  let introLockedHidden = true;

  servicesTop.addEventListener("toggle", () => {
    subItems.forEach((item) => {
      item.open = false;
    });

    if (servicesTop.open) {
      intro.style.display = "none";
    } else if (!introLockedHidden) {
      intro.style.display = "block";
    }
  });

  reset.addEventListener("click", () => {
    if (introLockedHidden) {
      introLockedHidden = false;
      intro.style.display = "block";
    } else {
      introLockedHidden = true;
      intro.style.display = "none";
    }

    servicesTop.open = false;

    subItems.forEach((item) => {
      item.open = false;
    });
  });
});
/* ===== Services Intro / Hidden Reset ===== */
document.addEventListener("DOMContentLoaded", () => {
  const intro = document.querySelector("#services-intro");
  const servicesTop = document.querySelector('section[aria-label="Services"] > details');
  const subItems = document.querySelectorAll(
    'section[aria-label="Services"] details.disclosure-item'
  );
  const reset = document.querySelector("#services-reset");

  if (!intro || !servicesTop || !reset) return;

  let introLockedHidden = true;

  servicesTop.addEventListener("toggle", () => {
    subItems.forEach((item) => {
      item.open = false;
    });

    if (servicesTop.open) {
      intro.style.display = "none";
    } else if (!introLockedHidden) {
      intro.style.display = "block";
    }
  });

  reset.addEventListener("click", () => {
    if (introLockedHidden) {
      introLockedHidden = false;
      intro.style.display = "block";
    } else {
      introLockedHidden = true;
      intro.style.display = "none";
    }

    servicesTop.open = false;

    subItems.forEach((item) => {
      item.open = false;
    });
  });
});
```

## FILE: src/js/forms.js

```
function loadCalEmbed() {
  const target = document.querySelector("#my-cal-inline-consultation");
  if (!target || target.dataset.loaded === "true") return;

  target.dataset.loaded = "true";

  (function (C, A, L) {
    let p = function (a, ar) { a.q.push(ar); };
    let d = C.document;

    C.Cal = C.Cal || function () {
      let cal = C.Cal;
      let ar = arguments;

      if (!cal.loaded) {
        cal.ns = {};
        cal.q = cal.q || [];
        d.head.appendChild(d.createElement("script")).src = A;
        cal.loaded = true;
      }

      if (ar[0] === L) {
        const api = function () { p(api, arguments); };
        const namespace = ar[1];
        api.q = api.q || [];

        if (typeof namespace === "string") {
          cal.ns[namespace] = cal.ns[namespace] || api;
          p(cal.ns[namespace], ar);
          p(cal, ["initNamespace", namespace]);
        } else {
          p(cal, ar);
        }

        return;
      }

      p(cal, ar);
    };
  })(window, "https://app.cal.com/embed/embed.js", "init");

  setTimeout(() => {
    Cal("init", "consultation", { origin: "https://app.cal.com" });

    Cal.ns.consultation("inline", {
      elementOrSelector: "#my-cal-inline-consultation",
      config: {
        layout: "month_view",
        useSlotsViewOnSmallScreen: true
      },
      calLink: "jchackett/consultation"
    });

    Cal.ns.consultation("ui", {
      hideEventTypeDetails: false,
      layout: "month_view"
    });
  }, 200);
}

/* ===== Get Started Form Reveal ===== */
(function getStartedReveal() {
  const button = document.querySelector("#get-started-btn");
  const formBlock = document.querySelector("#get-started-form");

  if (!button || !formBlock) return;

  button.addEventListener("click", function () {
    formBlock.hidden = false;
    button.style.display = "none";
  });
})();

/* ===== Consulting Form: Inline Success State ===== */
(function consultFormController() {
  const form = document.querySelector(".consult-form");
  const capture = document.querySelector(".consult-capture");
  const success = document.querySelector(".consult-success");
  const intro = document.querySelector(".consult-intro");
  const header = document.querySelector("#contact-me > summary");

  if (!form || !capture || !success) return;

  form.addEventListener("submit", function (e) {
    e.preventDefault();

    if (intro) intro.style.display = "none";

    capture.hidden = true;
    success.hidden = false;

    if (header) {
      header.textContent = "SCHEDULE A CONSULT";
    }

    loadCalEmbed();
  });
})(); 
```

## FILE: ERASMUS/ERASMUS/_kernels/kernel-20260310-130828.md

```
# Erasmus Kernel Snapshot
Generated: Tue Mar 10 13:08:28 WEST 2026
Project root: /c/Users/jerem/jchackett.me/ERASMUS

Max size: 50000 bytes

## DIRECTORY STRUCTURE\n\n```\n.
ERASMUS
Erasmus-CRITICAL.md
Erasmus-FEEDBACK.md
Erasmus_README.md
Erasmus_REBUILD.sh
_kernels
_kernels/kernel-0001.md
_kernels/kernel-0002.md
_kernels/kernel-0003.md
_kernels/kernel-0004.md
_kernels/kernel-0005.md
_kernels/kernel-0006.md
_kernels/kernel-0007.md
_kernels/kernel-20260309-132407.md
_kernels/kernel-20260310-094609.md
```\n\n
```

## FILE: ERASMUS/Erasmus_README.md

```
Hey there. I'm Erasmus. I'm the input text being used to help Jeremiah "hand code" his website. I bundle his website code so he can import it into LLM projects, and I help him execute coding for elements to his website and in general run his stack. I'm programmed to be light-hearted and direct. While I mostly give answers, I offer short details as to WHY things need to be (withouth going into too much detail).
```

## FILE: ERASMUS/Erasmus_REBUILD.sh

```
LOG_FILE="$KERNEL_DIR/kernel-build.log"

exec > >(tee -a "$LOG_FILE") 2>&1

echo "--------------------------------"
echo "Kernel build started: $(date)"
echo "Project root: $PROJECT_ROOT"
echo "Log file: $LOG_FILE"
echo "--------------------------------"

#!/usr/bin/env bash
set -euo pipefail
trap 'echo "ERROR on line $LINENO"; exit 1' ERR

MAX_BYTES=50000

PROJECT_ROOT="$(pwd)"
ERASMUS_DIR="$PROJECT_ROOT/ERASMUS"
KERNEL_DIR="$ERASMUS_DIR/_kernels"

mkdir -p "$KERNEL_DIR"

OUT_FILE="$KERNEL_DIR/kernel-$(date +%Y%m%d-%H%M%S).md"

echo "Starting Erasmus kernel build..."
echo "Writing kernel to $OUT_FILE"

current_size() {
  wc -c < "$OUT_FILE" | tr -d ' '
}

can_fit() {
  local add_bytes="$1"
  local now
  now="$(current_size)"
  [ $((now + add_bytes)) -le "$MAX_BYTES" ]
}

append_text() {
  local text="$1"
  local bytes
  bytes=$(printf "%s" "$text" | wc -c | tr -d ' ')
  if can_fit "$bytes"; then
    printf "%s" "$text" >> "$OUT_FILE"
    return 0
  fi
  return 1
}

append_file() {
  local file="$1"
  local rel="${file#$PROJECT_ROOT/}"
  local tmp
  tmp="$(mktemp)"

  {
    printf "## FILE: %s\n\n" "$rel"
    printf '```\n'
    sed 's/\r$//' "$file"
    printf '\n```\n\n'
  } > "$tmp"

  local bytes
  bytes=$(wc -c < "$tmp" | tr -d ' ')

  if can_fit "$bytes"; then
    echo "Exporting $rel"
    cat "$tmp" >> "$OUT_FILE"
  else
    echo "Skipping $rel (size cap)"
    append_text "## SKIPPED: $rel\n\nSkipped to keep kernel under ${MAX_BYTES} bytes.\n\n"
  fi

  rm -f "$tmp"
}

is_text_file() {
  case "$1" in
    *.md|*.njk|*.html|*.css|*.js|*.json|*.toml|*.yml|*.yaml|*.sh|*.cmd|*.txt)
      return 0
      ;;
    *)
      return 1
      ;;
  esac
}

{
  echo "# Erasmus Kernel Snapshot"
  echo "Generated: $(date)"
  echo "Project root: $PROJECT_ROOT"
  echo
  echo "Max size: ${MAX_BYTES} bytes"
  echo
} > "$OUT_FILE"

append_text "## DIRECTORY STRUCTURE\n\n\`\`\`\n"

find . \
  -path "./node_modules" -prune -o \
  -path "./_site" -prune -o \
  -path "./.git" -prune -o \
  -path "./ERASMUS/_kernels" -prune -o \
  -print | sed 's#^\./##' | while IFS= read -r line; do
    append_text "$line"$'\n' || break
  done

append_text "\`\`\`\n\n"

echo "Exporting src..."
find "$PROJECT_ROOT/src" -type f | sort | while IFS= read -r file; do
  is_text_file "$file" && append_file "$file"
done

echo "Exporting ERASMUS..."
find "$PROJECT_ROOT/ERASMUS" \
  -path "$PROJECT_ROOT/ERASMUS/_kernels" -prune -o \
  -type f -print | sort | while IFS= read -r file; do
    is_text_file "$file" && append_file "$file"
done

echo "Exporting root config..."
for file in \
  "$PROJECT_ROOT/.eleventy.js" \
  "$PROJECT_ROOT/.eleventyignore" \
  "$PROJECT_ROOT/netlify.toml" \
  "$PROJECT_ROOT/package.json" \
  "$PROJECT_ROOT/package-lock.json" \
  "$PROJECT_ROOT/.gitignore" \
  "$PROJECT_ROOT/README.md"
do
  [ -f "$file" ] && append_file "$file"
done

append_text "---\n\nKernel complete: $(basename "$OUT_FILE")\n"

echo
echo "Kernel written:"
echo "$OUT_FILE"
echo "Size: $(current_size) bytes"
```

## FILE: ERASMUS/Erasmus-CRITICAL.md

```
Keep a running note of all side items directed by user. When asked to take a note or mark something, just mark it. Add it to feedback.

When user indicates he's ready to escape, offer to generate 100 word document.

The project uses a simple, layered structure that separates site content, reusable components, styling, and process infrastructure. All site source files live inside `src`, which is Eleventy’s input directory. Layouts are in `src/_layouts`, reusable interface components live in `src/_includes/components`, and page content such as `index.md` sits at the root of `src`. Styling is organized under `src/css`, with component-specific styles placed in `src/css/components`. The disclosure interface is implemented through `disclosure-group.njk` and styled by `disclosure.css`, while `services.njk` and `contact.njk` provide content wrappers. Outside `src`, the `ERASMUS` directory stores kernel snapshots, process documentation, and rebuild scripts that preserve project memory between development iterations.

Project has moved from a cloned “menu” pattern to a coherent disclosure system. Current architecture is clean: `disclosure-group.njk` owns structure, `disclosure.css` owns styling, and `services.njk` / `contact.njk` act as content wrappers. Naming consistency materially improved readability and reduced mental context switching. Main lesson from this round: local visual debugging stayed manageable when changes were kept scoped and ownership boundaries were clarified rather than expanded. Future work should preserve this separation. Do not reintroduce duplicate component markup. When adjusting spacing or behavior, change the shared disclosure layer first. Verify `base.njk` stylesheet links whenever a component CSS file is renamed.

The site is designed to present information calmly and clearly, without visual clutter or navigational complexity. Visitors arrive on a single page where the essential introduction appears immediately, followed by structured sections that reveal additional information only when requested. The disclosure interface allows users to expand topics such as services or contact details at their own pace, keeping the page visually quiet while still providing depth. Typography, spacing, and restrained color choices emphasize readability and focus. The experience should feel deliberate, respectful of the visitor’s attention, and easy to navigate on both desktop and mobile, allowing users to quickly understand the purpose of the site and decide whether to reach out.

The most effective collaboration happens when instructions are concrete, scoped, and tied to specific files. When requesting changes, specify the file, the goal, and whether the task is structural, visual, or diagnostic. During visual debugging, small targeted changes are best; avoid asking for broad redesigns unless that is the explicit goal. When possible, paste the current contents of the file being modified so the assistant can work from the exact state of the project. Indicating whether you want explanation, implementation, or review also helps. Maintaining kernel snapshots and clear component ownership further improves continuity, allowing future iterations of the assistant to understand the project quickly and provide precise guidance.

```

## FILE: ERASMUS/Erasmus-FEEDBACK.md

```
Project has moved from a cloned “menu” pattern to a coherent disclosure system. Current architecture is clean: `disclosure-group.njk` owns structure, `disclosure.css` owns styling, and `services.njk` / `contact.njk` act as content wrappers. Naming consistency materially improved readability and reduced mental context switching. Main lesson from this round: local visual debugging stayed manageable when changes were kept scoped and ownership boundaries were clarified rather than expanded. Future work should preserve this separation. Do not reintroduce duplicate component markup. When adjusting spacing or behavior, change the shared disclosure layer first. Verify `base.njk` stylesheet links whenever a component CSS file is renamed.

```

## FILE: .eleventy.js

```
/* ===== Eleventy Config ===== */
module.exports = function (eleventyConfig) {
  /* ===== Passthrough Copies ===== */
  eleventyConfig.addPassthroughCopy({ "src/css": "css" });
eleventyConfig.addPassthroughCopy({ "src/js": "js" });
  eleventyConfig.addPassthroughCopy({ "src/images": "images" });

  /* ===== Ignore Non-Site Files ===== */
  eleventyConfig.ignores.add("README.md");
  eleventyConfig.ignores.add("_kernels/**");

  /* ===== Return Options ===== */
  return {
    markdownTemplateEngine: "njk",
    htmlTemplateEngine: "njk",
    dataTemplateEngine: "njk",
    dir: {
      input: "src",
      includes: "_includes",
      layouts: "_layouts",
      output: "_site",
    },
  };
};
/* ===== /Eleventy Config ===== */
```

## FILE: .eleventyignore

```
# ===== Eleventy ignores =====
README.md
_kernels/**
```

## FILE: netlify.toml

```
[build]
  command = "npm ci && npm run build"
  publish = "_site"

[build.environment]
  NPM_FLAGS = "--include=dev"
  NODE_VERSION = "20"
```

## FILE: package.json

```
{
  "name": "jchackett.me",
  "private": true,
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
```

## SKIPPED: package-lock.json\n\nSkipped to keep kernel under 50000 bytes.\n\n## FILE: .gitignore

```
node_modules/
_site/
.env
.DS_Store
package-lock.json
```

## FILE: README.md

```

```

---\n\nKernel complete: kernel-20260312-135147.md\n